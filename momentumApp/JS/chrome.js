// function resizePopupToBrowserSize() {
//     const popupWidth = window.innerWidth;
//     const popupHeight = window.innerHeight;
//     const contentElement = document.querySelector('.content');
//     contentElement.style.width = `${popupWidth}px`;
//     contentElement.style.height = `${popupHeight}px`;
//   }

//   // 창 크기가 변경될 때마다 팝업 페이지 크기를 조정
//   window.addEventListener('resize', resizePopupToBrowserSize);

//   // 초기에 한번 크기 조정 실행
//   resizePopupToBrowserSize();